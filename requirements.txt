# GlowUp — LIFX Effect Engine
#
# No third-party pip dependencies. The entire stack uses only the Python
# standard library. This file exists for conventional completeness and
# to confirm that "pip install -r requirements.txt" is a no-op.
#
# Linux users who want the --sim live preview need tkinter, which is a
# system package (not pip-installable):
#
#   sudo apt install python3-tk
